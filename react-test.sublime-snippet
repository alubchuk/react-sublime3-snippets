<snippet>
    <content><![CDATA[
import React from 'react';
import { shallow } from 'enzyme';
import ${1:MyComponent} from './$1';

const setup = () => {
  const props = {
    
  };
  const component = shallow(<$1 {...props} />);
  return { props, component };
};

export default describe('<$1>', () => {
  it('Should render a component without any errors', () => {
    const { component } = setup();
    expect(component).toMatchSnapshot();
  });
});

]]></content>
    
    <tabTrigger>rt</tabTrigger>
    <scope>source.js</scope>
    <scope>source.jsx</scope>
    <description>React Component Test</description>
</snippet>
